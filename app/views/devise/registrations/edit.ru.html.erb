<div class='row'>
  <div class='col-md-6'>
    <h2>Редактирование профиля</h2>

    <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
      <%= devise_error_messages! %>

      <div class="form-group">
        <%= f.label :email %><br />
        <%= f.email_field :email, autofocus: true, class: 'form-control' %>
      </div>

      <div class="form-group">
        <%= f.label :nickname, 'Никнэйм' %><br />
        <%= f.text_field :nickname, class: 'form-control' %>
      </div>

      <div class="form-group">
        <%= f.label :bio, 'О себе' %><br />
        <%= f.text_area :bio, class: 'form-control' %>
      </div>

      <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
        <div>В данный момент ожидается подтверждение для: <%= resource.unconfirmed_email %></div>
      <% end %>

      <div class="form-group">
        <%= f.label :password, 'Пароль' %> <i>(оставьте пустым что бы не менять)</i><br />
        <%= f.password_field :password, autocomplete: "off", class: 'form-control' %>
      </div>

      <div class="form-group">
        <%= f.label :password_confirmation, 'Подтвердите пароль' %><br />
        <%= f.password_field :password_confirmation, autocomplete: "off", class: 'form-control' %>
      </div>

      <div class="form-group">
        <%= f.label :current_password, 'Текущий пароль' %> <i>(нужен для подтверждения ваших текущих изменений)</i><br />
        <%= f.password_field :current_password, autocomplete: "off", class: 'form-control' %>
      </div>

      <div class="form-group">
        <%= f.submit "Сохранить", class: 'btn btn-success btn-sm' %>
      </div>
    <% end %>

    <h3>Удалить мой аккаунт</h3>

    <p><%= button_to "Удалить", registration_path(resource_name), data: { confirm: "Вы уверены? Это действие нельзя будет отменить" }, method: :delete, class: 'btn btn-danger btn-sm' %></p>

    <%#= link_to "Назад", :back %>
  </div>
</div>

